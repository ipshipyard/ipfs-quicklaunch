{
  "name": "ipfs-quicklaunch-extension",
  "version": "1.0.0",
  "description": "IPFS launcher browser extension with local pet names, version management, and smart CID discovery",
  "main": "src/background.js",
  "scripts": {
    "build": "tsc && cp src/manifest.json dist/ && cp src/popup.html dist/ && cp -r src/icons dist/",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.248",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "browser-extension",
    "chrome-extension",
    "ipfs-quicklaunch",
    "petnames"
  ],
  "author": "",
  "license": "MIT"
}